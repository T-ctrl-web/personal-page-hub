# PersonalPage Hub · 部署上线指南

本项目是 **React + Vite 纯静态站**，构建产物是纯静态文件，且使用 **hash 路由（`#/...`）**，因此**任何静态托管都零配置可用**。以下按推荐顺序给出三种部署方式。

---

## 方式一：GitHub Pages（推荐，免费）

### 1. 推送代码到 GitHub

```powershell
cd D:\DeepSeek-Harness\workplace\personal-page-hub
git init -b main
git add -A
git commit -m "feat: PersonalPage Hub MVP"
gh repo create personal-page-hub --public --source . --push
```

> 需要已登录 GitHub CLI：`gh auth login`。

### 2. 添加自动构建脚本

在项目根目录创建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - uses: actions/deploy-pages@v4
```

提交并推送：

```powershell
git add .github
git commit -m "ci: add GitHub Pages deploy workflow"
git push
```

### 3. 开启 Pages

- GitHub 仓库 → **Settings → Pages**
- Source 选择 **GitHub Actions**
- 首次部署由上面的 workflow 自动完成（Actions 页面可看进度）

### 4. 完成 🎉

访问：`https://<你的用户名>.github.io/personal-page-hub/`

以后每次 `git push`，网站自动重新构建部署。

### 可选：绑定自定义域名

仓库 Settings → Pages → Custom domain 填入你的域名，并在域名服务商加一条 CNAME 记录指向 `<用户名>.github.io`。

---

## 方式二：Vercel / Netlify（全球 CDN，也免费）

1. 把代码推到 GitHub（同上第 1 步）
2. 打开 [vercel.com](https://vercel.com) 或 [netlify.com](https://netlify.com)，用 GitHub 登录
3. **Import 该仓库**，框架自动识别 Vite
4. 构建命令：`pnpm build`；输出目录：`dist`
5. 点 Deploy，几分钟后拿到 `https://xxx.vercel.app` 地址

优点：无需写 Actions；每次 push 自动部署。

---

## 方式三：Gitee Pages（国内访问快）

1. 把代码推到 [gitee.com](https://gitee.com) 仓库
2. 仓库 → **服务 → Gitee Pages** → 选择分支 `main`、目录 `/`
3. 首次需实名认证，部署有人工审核（一般 1 天内）

> 注意：Gitee Pages 需要构建。可先把 `pnpm build` 后的 `dist/` 内容推到仓库（或部署时选「强制 HTTPS」并手动上传产物）。

---

## 常见问题

| 问题 | 解决 |
|---|---|
| 访问 404 | 确认 Pages 已开启且 Actions 部署成功；`base: './'` 已配好，无需额外设置 |
| 刷新子页面 404 | 我们是 hash 路由，不存在此问题（URL 带 `#/`） |
| 更新网站 | 改代码 → `git push` → Actions 自动部署 |
| 只想部署单文件版 | 用 `PersonalPageHub-单文件版.html`：把该文件重命名为 `index.html` 放到任意静态托管目录即可（如 GitHub 仓库根目录 + Pages 选择分支） |
| 本地预览构建产物 | `pnpm preview` 后访问 http://localhost:4173 |

## 本地构建备忘

```powershell
pnpm install      # 安装依赖
pnpm build        # 构建到 dist/（当前是单文件版 dist/index.html）
pnpm dev          # 开发模式（热更新）
```
